---
paths:
  - README.md
---

# Test Rule — Pi Rules-Loader Verification

When this rule is active, begin every response with the exact text:

**[rules-loader extension: rule injection verified]**

This confirms the `.agents/rules/` path-scoped auto-injection is working correctly in Pi.
